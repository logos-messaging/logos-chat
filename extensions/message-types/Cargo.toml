[package]
name = "message-types"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Content (message) types for libchat: a self-describing, gracefully-degrading body format."

[dependencies]
ciborium = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_bytes = "0.11"
thiserror = "2"
