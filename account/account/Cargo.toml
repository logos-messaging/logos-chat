[package]
name = "account"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Authoring, publishing, and resolving account logs."

[dependencies]
account-log = { path = "../account-log" }
thiserror = "2"
