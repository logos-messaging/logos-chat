[package]
name = "storage"
version = "0.1.0"
edition = "2024"
license.workspace = true
description = "Shared storage layer for libchat"

[dependencies]
# External dependencies (sorted)
thiserror = "2"
